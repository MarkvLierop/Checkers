package domain.enums;

import com.google.gson.Gson;
import com.google.gson.JsonObject;

public enum Action {
    NEW_GAME,
    MOVE_CHECKER,
    START_GAME,
    ERROR_MESSAGE,
    GAME_ENDED,
    CLOSE_CONNECTION
}
